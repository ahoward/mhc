# generated build output (CI rebuilds and deploys this)
/build
/tmp

# ruby / bundler
/vendor/bundle
/.bundle
*.gem

# node (favicon build pipeline uses sharp; not committed)
/node_modules

# env / secrets
.env
.env*.local
.kee

# vercel (legacy — being retired)
.vercel

# os
.DS_Store
*.pem

# claude code local config
/.claude
